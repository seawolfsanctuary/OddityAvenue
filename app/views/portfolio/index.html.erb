<h2><%= I18n.t('page_titles.portfolio') -%></h2>

<div id="portfolio-gallery" class="span12">
<%- @items.each do |i| -%>
  <div id="portfolio-thumb" class="span3">
    <%= link_to image_tag("portfolio/" + i.thumbnail_filename), portfolio_path(i.id) %>
    <p><%= i.title -%></p>
  </div>
<%- end -%>
</div>
